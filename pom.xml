<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<groupId>io.github.ctgnz</groupId>
	<artifactId>jmsfx</artifactId>
	<version>2.0.0-SNAPSHOT</version>
	<name>JMSFX - Joint Military Symbology on JavaFX</name>
	<description>A JavaFX implementation of NATO APP-6E military symbology: a generated domain model of the standard, and the SVG composition that draws a symbol from it.</description>
	<packaging>pom</packaging>
    <url>https://www.ctg.co.nz/jmsfx</url>

    <licenses>
        <license>
            <name>Apache License, Version 2.0</name>
            <url>https://www.apache.org/licenses/LICENSE-2.0.txt</url>
        </license>
    </licenses>
    
    <developers>
        <developer>
            <name>Colin Sharples</name>
            <email>ctg@ctg.co.nz</email>
            <organization>CTG Games Ltd</organization>
            <organizationUrl>http://www.ctg.co.nz</organizationUrl>
        </developer>
    </developers>
    
    <scm>
        <connection>scm:git:git://github.com/ctgnz/jmsfx.git</connection>
        <developerConnection>scm:git:ssh://github.com:ctgnz/jmsfx.git</developerConnection>
        <url>http://github.com/ctgnz/jmsfx/tree/master</url>
    </scm>

	<modules>
		<module>jmsfx-core</module>
		<module>jmsfx-generator</module>
		<module>jmsfx-standard</module>
		<module>jmsfx-hallux</module>
		<module>jmsfx-editor</module>
		<module>jmsfx-creator</module>
		<module>jmsfx-server</module>
	</modules>
	
	<properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <!-- Declared once here: jmsfx-core and jmsfx-standard both depend on foxglove and must agree. -->
        <foxglove.version>0.12.0</foxglove.version>
        <!-- Likewise for JavaFX, which jmsfx-standard declares directly and jmsfx-server pins again for
             its linux natives. These have to match each other and what foxglove pulls transitively, or
             the classpath ends up carrying two JavaFX versions at once. Stay on an LTS line (11, 17,
             21, 25) to match the JDK the server runs on. -->
        <javafx.version>25.0.3</javafx.version>
    </properties>
    
    <profiles>
        <profile>
            <id>release</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-javadoc-plugin</artifactId>
                        <configuration>
                            <!--
                                Keep doclint checking syntax, html and references - it is what caught a {@link} to a
                                type the file never imported, the release profile having never been run before - but
                                drop "missing", which reports every undocumented getter and buried that one error
                                under a hundred warnings.
                            -->
                            <doclint>all,-missing</doclint>
                        </configuration>
                        <executions>
                            <execution>
                                <id>attach-javadoc</id>
                                <goals>
                                    <goal>jar</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-source-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>attach-sources</id>
                                <goals>
                                    <goal>jar</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <!--
                            Central requires signed artifacts. Signing at verify rather than at deploy so the
                            signatures exist by the time the publishing plugin builds its bundle, and
                            loopback pinentry so CI can sign unattended with the passphrase in the environment.
                        -->
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-gpg-plugin</artifactId>
                        <version>3.1.0</version>
                        <executions>
                            <execution>
                                <id>sign-artifacts</id>
                                <phase>verify</phase>
                                <goals>
                                    <goal>sign</goal>
                                </goals>
                                <configuration>
                                    <gpgArguments>
                                        <arg>--pinentry-mode</arg>
                                        <arg>loopback</arg>
                                    </gpgArguments>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <!--
                            Publishes to the Central Portal, which replaced OSSRH - hence no distributionManagement
                            above, since the plugin uploads directly rather than deploying to a staging repository.
                            
                            autoPublish is false, matching foxglove: a tag stages a validated deployment on the
                            Portal and publishing stays a deliberate step, so a bad staging can be dropped rather
                            than having to be superseded.
                            
                            Only the parent, jmsfx-core and jmsfx-standard belong on Central: the API a consumer
                            compiles against and the generated library it renders with. The parent is easy to
                            overlook and resolution breaks for everyone without it. The rest are excluded -
                            creator, editor and server are applications, delivered as bundles and a deployment,
                            and the generator only becomes a published artifact once jmsfx#102 gives the library
                            repositories something to depend on when they generate themselves.
                        -->
                        <groupId>org.sonatype.central</groupId>
                        <artifactId>central-publishing-maven-plugin</artifactId>
                        <version>0.11.0</version>
                        <extensions>true</extensions>
                        <configuration>
                            <publishingServerId>central</publishingServerId>
                            <autoPublish>false</autoPublish>
                            <excludeArtifacts>
                                <excludeArtifact>jmsfx-generator</excludeArtifact>
                                <excludeArtifact>jmsfx-creator</excludeArtifact>
                                <excludeArtifact>jmsfx-editor</excludeArtifact>
                                <excludeArtifact>jmsfx-server</excludeArtifact>
                            </excludeArtifacts>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>

	<build>
        <sourceDirectory>src/main/java</sourceDirectory>
        <resources>
            <resource>
                <directory>src/main/resources</directory>
            </resource>
        </resources>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-javadoc-plugin</artifactId>
                    <version>3.5.0</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-source-plugin</artifactId>
                    <version>3.3.0</version>
                </plugin>
                <!-- Tells m2e to skip Spotless during its own incremental/full builds, rather than fail with
                     "Plugin execution not covered by lifecycle configuration" - `mvn` from the command line still
                     runs it normally, since pluginManagement doesn't affect that. -->
                <plugin>
                    <groupId>org.eclipse.m2e</groupId>
                    <artifactId>lifecycle-mapping</artifactId>
                    <version>1.0.0</version>
                    <configuration>
                        <lifecycleMappingMetadata>
                            <pluginExecutions>
                                <pluginExecution>
                                    <pluginExecutionFilter>
                                        <groupId>com.diffplug.spotless</groupId>
                                        <artifactId>spotless-maven-plugin</artifactId>
                                        <versionRange>[2.44.5,)</versionRange>
                                        <goals>
                                            <goal>check</goal>
                                        </goals>
                                    </pluginExecutionFilter>
                                    <action>
                                        <ignore/>
                                    </action>
                                </pluginExecution>
                                <pluginExecution>
                                    <pluginExecutionFilter>
                                        <groupId>org.apache.maven.plugins</groupId>
                                        <artifactId>maven-dependency-plugin</artifactId>
                                        <versionRange>[3.7.0,)</versionRange>
                                        <goals>
                                            <goal>copy-dependencies</goal>
                                        </goals>
                                    </pluginExecutionFilter>
                                    <action>
                                        <ignore/>
                                    </action>
                                </pluginExecution>
                                <!-- The fragment check (jmsfx-generator, #73) walks ~3,000 files, which is
                                     not something to do on every incremental build in the IDE. `mvn verify`
                                     still runs it. -->
                                <pluginExecution>
                                    <pluginExecutionFilter>
                                        <groupId>org.codehaus.mojo</groupId>
                                        <artifactId>exec-maven-plugin</artifactId>
                                        <versionRange>[3.6.4,)</versionRange>
                                        <goals>
                                            <goal>java</goal>
                                        </goals>
                                    </pluginExecutionFilter>
                                    <action>
                                        <ignore/>
                                    </action>
                                </pluginExecution>
                            </pluginExecutions>
                        </lifecycleMappingMetadata>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.11.0</version>
                <configuration>
                    <source>25</source>
                    <target>25</target>
                    <encoding>UTF-8</encoding>
                    <parameters>true</parameters>
                    <compilerArgs>
                        <arg>-Xlint:all</arg>
                        <arg>-Werror</arg>
                    </compilerArgs>
                </configuration>
            </plugin>
            <plugin>
                <!-- The previous default predates the JUnit Platform entirely and cannot discover Jupiter tests. -->
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.6.0</version>
            </plugin>
            <plugin>
                <groupId>com.diffplug.spotless</groupId>
                <artifactId>spotless-maven-plugin</artifactId>
                <version>2.44.5</version>
                <configuration>
                    <java>
                        <importOrder>
                            <file>${maven.multiModuleProjectDirectory}/config/eclipse.importorder</file>
                        </importOrder>
                        <removeUnusedImports/>
                        <eclipse>
                            <file>${maven.multiModuleProjectDirectory}/config/eclipse-formatter.xml</file>
                        </eclipse>
                        <trimTrailingWhitespace/>
                    </java>
                </configuration>
                <executions>
                    <execution>
                        <goals>
                            <goal>check</goal>
                        </goals>
                        <phase>verify</phase>
                    </execution>
                </executions>
            </plugin>
        </plugins>
	</build>

</project>